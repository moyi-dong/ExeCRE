# ExeCRE (Anonymous Repository)

This is the anonymous repository of ExeCRE.

The core components are already included here.

Some additional parts are still required to make it fully runnable, and they are currently being added.

```
conda create -n execre python=3.11 -y
conda activate execre-test
cd ExeCRE
pip install -e .
```

大模型api配置
```
export DEEPSEEK_API_KEY="sk-xxxx"
```