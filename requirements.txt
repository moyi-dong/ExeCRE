# Mirrors pyproject.toml [project.dependencies].
#   pip install -r requirements.txt
# Prefer editable install (registers `src` and vendored `textgrad`):
#   pip install -e .

anthropic>=0.42.0
crowdkit>=1.3.0
httpx>=0.27.0
litellm>=1.50.0
loguru>=0.7.0
numpy>=1.24.0
openai>=1.59.0
pandas>=2.0.0
platformdirs>=4.0.0
pyyaml>=6.0.0
requests>=2.31.0
tenacity>=8.2.0
tqdm>=4.66.0
